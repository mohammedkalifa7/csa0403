#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>

int main() {
    int fd1, fd2;
    char buffer[100];
    int n;

    fd1 = open("source.txt", O_RDONLY);
    fd2 = open("dest.txt", O_CREAT | O_WRONLY, 0644);

    while ((n = read(fd1, buffer, 100)) > 0) {
        write(fd2, buffer, n);
    }

    close(fd1);
    close(fd2);

    printf("File copied successfully\n");
    return 0;
}
